import React from "react";
import classes from "./TimeTable.module.css";

const TimeTable = (props) => (
  <section className={classes.time_table}>
    <h2 className={classes.title}>Time Table</h2>
  </section>
);

export default TimeTable;
